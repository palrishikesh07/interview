{
  "name": "graphql",
  "version": "1.0.0",
  "description": "- GraphQL is an API <b>query language</b> and runtime that lets a client request exactly the data it needs.  - It was originally developed at Meta and is now maintained by the GraphQL Foundation. - The three core operations are: -  Queries (read data). -  Mutations (change data). -  Subscriptions (receive real-time updates).",
  "main": "1.js",
  "scripts": {
    "start":"nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@apollo/server": "^5.5.1",
    "graphql": "^16.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
